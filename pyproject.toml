[tool.poetry]
name = "account-managment"
version = "0.1.0"
description = ""
authors = ["Flavio Oria <flavio.oriap@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.111.0"
raw_dbmodel = { git = "https://github.com/flaviooria/raw_dbmodel.git", tag="0.2.3", extras = ["psycopg2"] }
alembic = "^1.13.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
